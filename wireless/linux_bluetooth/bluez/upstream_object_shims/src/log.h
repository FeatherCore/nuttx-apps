#ifndef BLUEZ_UPSTREAM_OBJECT_SHIM_LOG_H
#define BLUEZ_UPSTREAM_OBJECT_SHIM_LOG_H
#include <stdio.h>
#define DBG(fmt, ...) do { } while (0)
#define error(fmt, ...) do { } while (0)
#define info(fmt, ...) do { } while (0)
#define warn(fmt, ...) do { } while (0)
#endif
